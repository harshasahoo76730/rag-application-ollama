// Dev Container Configuration for RAG Environment using Ollama
{
  "name": "RAG Environment using Ollama",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--net=host"],
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python",
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "bash"
          }
        }
      }
    }
  }
}
